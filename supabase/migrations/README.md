# Migrations

Timestamped migrations generated by the Supabase CLI belong here.

This directory intentionally has no fabricated baseline migration. The linked
production database already exists, and creating a guessed migration history
would be misleading. Run the authenticated `supabase db pull` workflow in the
parent README before the first future deployment.
